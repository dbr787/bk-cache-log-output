# bk-cache-log-output

Small playground repository to prototype Buildkite log-output formats.

## Getting started
```
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt  # (empty for now)
```

## Buildkite pipeline (local test)
```
bk local run
```
